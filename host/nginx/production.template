upstream active {
    server ${ACTIVE}:3000;
}
upstream standby {
    server ${STANDBY}:3000;
}

server {
    location / {
        proxy_pass http://active;
    }
    location /stage {
        rewrite ^/stage/(.*)  /$1 break;
        proxy_pass http://standby;
    }
}
