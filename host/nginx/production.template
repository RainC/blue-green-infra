upstream active {
    server ${ACTIVE}:5000;
}
upstream standby {
    server ${STANDBY}:5000;
}

server {
    location / {
        proxy_pass http://active;
    }
    location /stage {
        rewrite ^/stage/(.*)  /$1 break;
        proxy_pass http://standby;
    }
}
